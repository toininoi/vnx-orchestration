[[TARGET:C]]
Manager Block

Role: backend-developer
Track: C
Terminal: T3
Gate: implementation
Priority: P2
Cognition: high
Dispatch-ID: 20260221-174530-pr6-implementation-C
PR-ID: PR-6
Parent-Dispatch: 20260221-165539-email-campaign-integration-(tr-C
On-Success: review
On-Failure: investigation
Reason: PR-6 implementation — Email Campaign Integration with GDPR compliance, following architecture review
Status: pending-approval

Context: [[@FEATURE_PLAN.md]]

Previous Deliverable: Architecture review at
`.vnx-data/unified_reports/20260221-174237-C-email-campaign-architecture-review.md` — use this as your design spec.

Instruction:

Implement Email Campaign Integration per the architecture review you delivered. Key requirements:

### 1. Persistence Layer (OI-052 blocker)
- Add lightweight SQLite persistence for consent records and campaign state
- Use `aiosqlite` or synchronous `sqlite3` with async wrapper
- Database file: `data/leadflow.db` (gitignored)
- Schema: `consent_records`, `campaigns`, `campaign_enrollments`, `ab_test_assignments`
- Consent records are append-only (never UPDATE/DELETE — GDPR Article 7(1) audit trail)

### 2. Consent Management (OI-034 blocker)
- `ConsentRecord` dataclass with: lead_id, email, consent_given, consent_timestamp, consent_source, consent_ip,
consent_version, withdrawn_at
- `verify_consent()` called before EVERY email send — no caching, no bypass
- Withdrawal via `withdrawn_at` timestamp (immutable records)
- Single opt-in for MVP (double opt-in deferred — OI-054)
- Endpoints: POST/DELETE/GET `/api/leads/{lead_id}/consent`, GET `/api/leads/{lead_id}/consent/export`

### 3. Campaign Service
- `src/services/campaign_service.py` — keep under 400 lines
- If approaching limit, decompose into `consent_manager.py`, `enrollment_engine.py`, `ab_testing.py`
- Score threshold enrollment using tier transitions (COLD→WARM, WARM→HOT) with hysteresis (10-point buffer)
- Template selection based on `ScoreTier` enum from `src/models/score.py`
- A/B testing: deterministic hash assignment, 2-variant only for v1, chi-squared significance test, manual winner
declaration

### 4. API Layer
- `src/api/campaigns.py` — campaign CRUD + send trigger
- Unsubscribe endpoint: `GET/POST /unsubscribe/{token}` — public, no auth, idempotent
- Wire into `src/api/main.py`

### 5. Models
- `src/models/campaign.py` — ConsentRecord, Campaign, CampaignTemplate, CampaignStatus, ABTestConfig, ABVariant
- CAN-SPAM: `unsubscribe_url` mandatory in every CampaignTemplate
- RFC 8058: List-Unsubscribe header support

### 6. Integration
- Do NOT modify `lead_scoring_engine.py` (already at 652 lines). Use event hook pattern — campaign service registers
a callback, scoring flow calls it externally.
- Wire real-time enrollment notifications through existing `ConnectionManager.broadcast_score_update()`

### Quality Gates (ALL must pass)
- [ ] All tests pass including E2E campaign enrollment flow (OI-031)
- [ ] No Python file exceeds 500 lines (warn) / 800 lines (blocker)
- [ ] No function exceeds 40 lines (warn) / 70 lines (blocker)
- [ ] 100% GDPR compliance — verify_consent() before every send, no bypass path (OI-034)
- [ ] Unsubscribe mechanism functional (OI-035)
- [ ] No email sent without explicit consent record (OI-036)
- [ ] Consent records persisted to SQLite, survive restart (OI-052)
- [ ] No PII in logs — log lead_id only, never email addresses

### Success Criteria
- `pytest tests/` — full suite passes, 0 regressions
- New test file(s) cover: consent CRUD, consent withdrawal halts sends, enrollment triggers on tier transition, A/B
deterministic assignment, unsubscribe flow, campaign CRUD API
- Include quality gate checklist in report with actual measurements

Workflow:
1. Create models (campaign.py)
2. Create persistence layer (SQLite schema + helpers)
3. Implement consent manager
4. Implement campaign service with enrollment + A/B
5. Create API endpoints
6. Wire into main.py
7. Write comprehensive tests
8. Run full test suite, verify all gates

[[DONE]]
