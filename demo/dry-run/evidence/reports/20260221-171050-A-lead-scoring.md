**Dispatch ID**: 20260221-165539-lead-scoring-engine-(track-a)-A
**PR**: PR-2
**Track**: A
**Gate**: implementation
**Status**: success

## Implementation Summary
- Rebuilt `LeadScoringEngine` to load YAML-controlled weights, normalize composite totals to 0-100, and emit audit entries with timestamps/reasons while tracking the selected AI registry provider/model.
- Added config (`config/scoring_weights.yaml`) plus shared score models (`src/models/score.py`) to expose the scoring enums/data classes used by both engine and tests.
- Implemented dimension-based scoring helpers, batch processing with progress callbacks and aggregated statistics, and a history tracker to log score changes.

## Files Modified
- `config/scoring_weights.yaml`: New YAML defaults for every tracked event weight, decay, caps, and recency boosts.
- `src/models/score.py`: Shared data classes/enums used by the engine and future consumers (includes AI provider/model metadata on `LeadScore`).
- `src/services/lead_scoring_engine.py`: Core engine replacement with config-driven weights, AI registry lookup, detailed audit records, batch processing/stats, and helper scoring utilities.
- `tests/test_lead_scoring_engine.py`: New unit tests verifying weight loading, normalization/audit, and batch summary behavior.

## Testing Evidence
- `pytest tests/test_lead_scoring_engine.py` (pass)

## Open Items
- None.
